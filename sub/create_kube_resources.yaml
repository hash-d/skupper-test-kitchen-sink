---

- name: Create Skupper resources
  kubernetes.core.k8s:
    kubeconfig: "{{ kubeconfig | default (omit) }}"
    state: present
    definition: "{{ item }}"
  loop: "{{ application_resources | default ([]) }}"
