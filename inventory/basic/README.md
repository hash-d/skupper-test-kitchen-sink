
Usage
=====

This is the basic inventory, with defaults or facts that s hould apply
to all inventories, such as:

- The default `ansible_host` pointing to `localhost`, that can then be
  overridden per host
- The `skupper_platform` setting for each of the system groups (docker
  podman and systemd)
