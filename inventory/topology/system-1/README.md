
Description
===========

This is a basic topology with two leaves connecting to a hub.

The Hub is a podman site, the leaves are docker and systemd.

The Hub has no outgoing links, so you can uses a connector topology to link it to another one.
