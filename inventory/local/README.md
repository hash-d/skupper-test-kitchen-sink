
Instructions
============

Other than this README.md file, all files and directories under this
directory are ignored by git.  Yet, the directory itself is always
included as an inventory source.

This is where you can configure `ansible_host` and kubeconfigs,
so the playbooks act on actual VMs instead of local host, or on multiple
kube clusters.
